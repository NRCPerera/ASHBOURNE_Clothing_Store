{
  "name": "ashbourne-clothing-store",
  "version": "1.0.0",
  "private": true,
  "description": "ASHBOURNE Clothing Store — Full-stack e-commerce platform",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:admin": "cd admin && npm run dev -- --port 5174",
    "install:all": "cd server && npm install && cd ../client && npm install && cd ../admin && npm install"
  }
}
